#pragma once

#include "core/prelude/object.hpp"

namespace cannele::rhi
{
    struct Fence
    {
        CN_INTERFACE(Fence);
    };

    struct Semaphore
    {
        CN_INTERFACE(Semaphore);
    };
}
